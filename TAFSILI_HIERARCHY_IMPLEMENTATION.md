# 📋 سیستم اتصال سلسله مراتبی تفصیلی

## 🎯 مقدمه و هدف

این سند پیاده‌سازی سیستم **اتصال سلسله مراتبی تفصیلی** را توضیح می‌دهد. هدف اصلی ایجاد امکان تعریف روابط parent-child بین حساب‌های تفصیلی به صورت درختی است.

### اهداف اصلی:
- ایجاد ساختار سلسله مراتبی بین حساب‌های تفصیلی
- حفظ سیستم فعلی ۳ سطح مستقل در سند حسابداری
- اضافه کردن فیلترینگ هوشمند در سند حسابداری بر اساس hierarchy
- امکان تعریف روابط چند سطحی (تا ۳ سطح)

## 🔍 وضعیت فعلی سیستم تفصیلی

### ساختار موجود:
1. **۳ سطح تفصیلی مستقل**: `tafsili_level_1`, `tafsili_level_2`, `tafsili_level_3`
2. **فیلترینگ فعلی**: فقط بر اساس نوع تفصیلی مجاز در حساب معین (`SubAccountTafsiliTypeRelation`)
3. **رابطه با سند**: هر سطح کاملاً مستقل انتخاب می‌شود
4. **validation**: سطح ۲ نیاز به سطح ۱ دارد، سطح ۳ نیاز به سطح ۲ دارد

### محدودیت‌های فعلی:
- هیچ رابطه‌ای بین حساب‌های تفصیلی وجود ندارد
- کاربر می‌تواند هر ترکیبی از سطوح را انتخاب کند
- ساختار درختی سازمانی وجود ندارد

## ✨ سیستم اتصال سلسله مراتبی جدید

### ساختار پیشنهادی:

```
تهران (سطح ۱)
├── تهران-منطقه۱ (سطح ۲)
│   ├── تهران-منطقه۱-شعبهA (سطح ۳)
│   └── تهران-منطقه۱-شعبهB (سطح ۳)
└── تهران-منطقه۲ (سطح ۲)
    └── تهران-منطقه۲-شعبهC (سطح ۳)
```

### ویژگی‌ها:
- **ساختار درختی**: هر حساب تفصیلی می‌تواند parent چندین حساب دیگر باشد
- **چند سطحی**: تا ۳ سطح پشتیبانی می‌شود
- **اختیاری**: حساب‌های تفصیلی می‌توانند در hierarchy باشند یا نباشند
- **انعطاف‌پذیر**: امکان تعریف روابط مختلف برای سازمان‌های مختلف

## 🔧 تغییرات فنی مورد نیاز

### ۱. مدل داده‌ای جدید

#### `TafsiliAccountHierarchy`
```python
class TafsiliAccountHierarchy(AccountingBaseModel):
    """
    روابط سلسله مراتبی بین حساب‌های تفصیلی
    """
    parent_account = models.ForeignKey(
        Account,
        on_delete=models.CASCADE,
        related_name='hierarchy_children',
        limit_choices_to={'account_level': 3},
        help_text="حساب تفصیلی والد"
    )
    child_account = models.ForeignKey(
        Account,
        on_delete=models.CASCADE,
        related_name='hierarchy_parents',
        limit_choices_to={'account_level': 3},
        help_text="حساب تفصیلی فرزند"
    )
    level_depth = models.PositiveSmallIntegerField(
        help_text="عمق رابطه در درخت (۱=مستقیم، ۲=نوه، ...)"
    )
    sort_order = models.PositiveSmallIntegerField(
        default=0,
        help_text="ترتیب نمایش"
    )

    class Meta:
        verbose_name = "رابطه سلسله مراتبی تفصیلی"
        verbose_name_plural = "روابط سلسله مراتبی تفصیلی"
        constraints = [
            models.UniqueConstraint(
                fields=("company", "parent_account", "child_account"),
                name="accounting_tafsili_hierarchy_unique",
            ),
        ]
```

### ۲. منطق فیلترینگ در سند حسابداری

#### لایه‌های فیلترینگ:
1. **لایه اول (موجود)**: فیلترینگ بر اساس نوع تفصیلی مجاز در معین
2. **لایه دوم (جدید)**: فیلترینگ بر اساس hierarchy

#### منطق فیلترینگ bidirectional:

**اگر سطح ۳ انتخاب شود:**
- سطح ۲ = parent مستقیم حساب انتخاب شده در سطح ۳
- سطح ۱ = parent سطح ۲

**اگر سطح ۲ انتخاب شود:**
- سطح ۱ = parent مستقیم حساب انتخاب شده در سطح ۲
- سطح ۳ = childهای مستقیم حساب انتخاب شده در سطح ۲

**اگر سطح ۱ انتخاب شود:**
- سطح ۲ = childهای مستقیم حساب انتخاب شده در سطح ۱
- سطح ۳ = childهای childهای حساب انتخاب شده در سطح ۱

### ۳. APIهای جدید مورد نیاز

#### `get-tafsili-hierarchy-info/`
- ورودی: `tafsili_account_id`
- خروجی: اطلاعات hierarchy (parentها و childها)

#### `get-filtered-tafsili-accounts/`
- ورودی: `sub_account_id`, `selected_level_1`, `selected_level_2`, `selected_level_3`
- خروجی: لیست فیلتر شده حساب‌های مجاز برای هر سطح

### ۴. تغییرات در Frontend

#### سند حسابداری (`documents/create.html`):
```javascript
// اضافه کردن listener برای تغییرات سطح تفصیلی
$('.line-tafsili-level-1, .line-tafsili-level-2, .line-tafsili-level-3').on('change', function() {
    applyHierarchyFiltering();
});

function applyHierarchyFiltering() {
    const level1 = $('.line-tafsili-level-1').val();
    const level2 = $('.line-tafsili-level-2').val();
    const level3 = $('.line-tafsili-level-3').val();

    // فراخوانی API برای گرفتن لیست فیلتر شده
    fetchFilteredTafsiliAccounts(level1, level2, level3);
}
```

### ۵. صفحه مدیریت اتصال سلسله مراتبی

#### عملکردهای مورد نیاز:
1. **نمایش ساختار درختی**: نمایش hierarchy به صورت tree view
2. **اضافه کردن رابطه**: انتخاب parent و child
3. **حذف رابطه**: حذف اتصال بین دو حساب
4. **جستجو و فیلتر**: امکان جستجو در حساب‌های تفصیلی
5. **اعتبارسنجی**: جلوگیری از circular reference

#### ساختار صفحه:
```
📊 اتصال سلسله مراتبی تفصیلی

[🔍 جستجو در حساب‌های تفصیلی]

📋 لیست روابط موجود:
├── تهران
│   ├── تهران-منطقه۱
│   │   ├── تهران-منطقه۱-شعبهA
│   │   └── تهران-منطقه۱-شعبهB
│   └── تهران-منطقه۲
│       └── تهران-منطقه۲-شعبهC

[+ اضافه کردن رابطه جدید]
[حذف] [ویرایش] برای هر رابطه
```

## 🚀 مراحل پیاده‌سازی

### فاز ۱: مدل پایه
1. ایجاد مدل `TafsiliAccountHierarchy`
2. migration و تست مدل

### فاز ۲: APIها
1. ایجاد API `get-tafsili-hierarchy-info/`
2. ایجاد API `get-filtered-tafsili-accounts/`
3. تست APIها

### فاز ۳: صفحه مدیریت
1. ایجاد view و template
2. پیاده‌سازی tree view
3. اضافه کردن/حذف رابطه

### فاز ۴: ادغام با سند حسابداری
1. تغییرات JavaScript در `documents/create.html`
2. تست فیلترینگ hierarchy

### فاز ۵: تست و بهینه‌سازی
1. تست سناریوهای مختلف
2. بررسی performance
3. مستندسازی

## ⚠️ ملاحظات امنیتی و اعتبارسنجی

### اعتبارسنجی داده‌ها:
- جلوگیری از circular reference
- بررسی وجود حساب‌ها در شرکت
- validation عمق hierarchy (حداکثر ۳ سطح)

### امنیت:
- بررسی دسترسی کاربر به حساب‌های تفصیلی
- validation داده‌های ورودی API
- جلوگیری از تغییر hierarchy توسط کاربران غیرمجاز

## 📈 مزایای سیستم جدید

1. **سازماندهی بهتر**: ساختار منطقی برای حساب‌های تفصیلی
2. **سرعت بیشتر**: فیلترینگ خودکار در سند حسابداری
3. **کاهش خطا**: جلوگیری از انتخاب‌های نادرست
4. **انعطاف‌پذیری**: امکان استفاده اختیاری از hierarchy
5. **قابلیت توسعه**: امکان اضافه کردن ویژگی‌های پیشرفته‌تر

## 🌳 تاثیر روی نمایش درختچه حساب‌ها

### چارت حساب‌ها (Chart of Accounts):
در صفحه چارت حساب‌ها، حساب‌های تفصیلی که دارای hierarchy هستند باید به صورت ساختار درختی نمایش داده شوند:

```
📊 چارت حساب‌ها
├── حساب کل ۱
│   └── حساب معین ۱.۱
│       ├── تفصیلی ۱.۱.۱ (بدون hierarchy)
│       ├── تفصیلی ۱.۱.۲ (بدون hierarchy)
│       └── تهران (با hierarchy)
│           ├── تهران-منطقه۱
│           │   ├── تهران-منطقه۱-شعبهA
│           │   └── تهران-منطقه۱-شعبهB
│           └── تهران-منطقه۲
│               └── تهران-منطقه۲-شعبهC
```

### مرور حساب‌ها (Account Browser):
در صفحه مرور حساب‌ها نیز ساختار hierarchy باید نمایش داده شود و امکان فیلتر بر اساس hierarchy وجود داشته باشد.

### تغییرات مورد نیاز:
1. **API جدید**: `get-tafsili-hierarchy-tree/` برای دریافت ساختار کامل درخت
2. **Template**: نمایش ساختار درختی در چارت حساب‌ها
3. **JavaScript**: قابلیت collapse/expand درخت hierarchy
4. **فیلترها**: امکان فیلتر بر اساس hierarchy در مرور حساب‌ها

## 🔍 مثال‌های عملی

### مثال ۱: شرکت با ساختار جغرافیایی
```
ایران (سطح ۱)
├── تهران (سطح ۲)
│   ├── تهران-شمالی (سطح ۳)
│   └── تهران-جنوبی (سطح ۳)
└── اصفهان (سطح ۲)
    └── اصفهان-مرکزی (سطح ۳)
```

### مثال ۲: شرکت با ساختار سازمانی
```
مدیریت (سطح ۱)
├── مالی (سطح ۲)
│   ├── حسابداری (سطح ۳)
│   └── بودجه (سطح ۳)
└── عملیاتی (سطح ۲)
    └── تولید (سطح ۳)
```

---

## 📝 یادداشت‌های پیاده‌سازی

- تاریخ شروع: [تاریخ]
- مسئول پیاده‌سازی: [نام]
- وضعیت: آماده برای شروع
- اولویت: بالا
